1. Reverse a string
function reverseString(str) {
  return str.split("").reverse().join("");
}
console.log(reverseString("hello")); 
// "olleh"

2. Check if a string is a palindrome
function isPalindrome(str) {
  let rev = str.split("").reverse().join("");
  return str === rev;
}
console.log(isPalindrome("madam")); // true
console.log(isPalindrome("hello")); // false

3. Factorial of a number
function factorial(n) {
  if (n === 0) return 1;
  return n * factorial(n - 1);
}
console.log(factorial(5)); 
// 120

4. Largest number in an array
function largest(arr) {
  return Math.max(...arr);
}
console.log(largest([2, 8, 5, 10, 3])); 
// 10

5. Smallest number in an array
function smallest(arr) {
  return Math.min(...arr);
}
console.log(smallest([2, 8, 5, 10, 3])); 
// 2

6. Count vowels in a string
function countVowels(str) {
  return str.match(/[aeiou]/gi)?.length || 0;
}
console.log(countVowels("hello world")); 
// 3

7. Sum of array elements
function sumArray(arr) {
  return arr.reduce((a, b) => a + b, 0);
}
console.log(sumArray([1, 2, 3, 4])); 
// 10

8. Average of array elements
function averageArray(arr) {
  return arr.reduce((a, b) => a + b, 0) / arr.length;
}
console.log(averageArray([1, 2, 3, 4])); 
// 2.5

9. Fibonacci series up to n
function fibonacci(n) {
  let arr = [0, 1];
  for (let i = 2; i < n; i++) {
    arr.push(arr[i - 1] + arr[i - 2]);
  }
  return arr;
}
console.log(fibonacci(7));

10. Length of the longest word
function longestWordLength(str) {
  return Math.max(...str.split(" ").map(word => word.length));
}
console.log(longestWordLength("I love programming in JavaScript")); 
// 11

🔹 Intermediate (11–20)
11. Remove duplicates from an array
function removeDuplicates(arr) {
  return [...new Set(arr)];
}
console.log(removeDuplicates([1,2,2,3,4,4,5])); 
// [1,2,3,4,5]

12. Find second largest number
function secondLargest(arr) {
  let unique = [...new Set(arr)].sort((a, b) => b - a);
  return unique[1];
}
console.log(secondLargest([10, 5, 8, 20, 20])); 
// 10

13. Missing number in sequence [1…n]
function missingNumber(arr, n) {
  let total = (n * (n + 1)) / 2;
  let sum = arr.reduce((a, b) => a + b, 0);
  return total - sum;
}
console.log(missingNumber([1,2,4,5], 5)); 
// 3

14. Check if two strings are anagrams
function isAnagram(a, b) {
  return a.split("").sort().join("") === b.split("").sort().join("");
}
console.log(isAnagram("listen", "silent")); // true
console.log(isAnagram("hello", "world"));   // false

15. Count character occurrences
function charCount(str) {
  let count = {};
  for (let ch of str) {
    count[ch] = (count[ch] || 0) + 1;
  }
  return count;
}
console.log(charCount("hello"));
// { h:1, e:1, l:2, o:1 }

16. Intersection of two arrays
function intersection(arr1, arr2) {
  return arr1.filter(x => arr2.includes(x));
}
console.log(intersection([1,2,3], [2,3,4])); 
// [2,3]

17. Reverse words in a sentence
function reverseWords(str) {
  return str.split(" ").reverse().join(" ");
}
console.log(reverseWords("I love JavaScript")); 
// "JavaScript love I"

18. First non-repeated character
function firstNonRepeated(str) {
  for (let ch of str) {
    if (str.indexOf(ch) === str.lastIndexOf(ch)) {
      return ch;
    }
  }
  return null;
}
console.log(firstNonRepeated("swiss")); 
// "w"

19. Check if array is sorted
function isSorted(arr) {
  for (let i = 1; i < arr.length; i++) {
    if (arr[i] < arr[i - 1]) return false;
  }
  return true;
}
console.log(isSorted([1,2,3,4])); // true
console.log(isSorted([1,3,2]));   // false

20. Rotate array by k
function rotateArray(arr, k) {
  k %= arr.length;
  return arr.slice(-k).concat(arr.slice(0, -k));
}
console.log(rotateArray([1,2,3,4,5], 2)); 
// [4,5,1,2,3]

🔹 Advanced (21–30)
21. Flatten a nested array (infinity depth)
function flatten(arr) {
  return arr.flat(Infinity);
}
console.log(flatten([1,[2,[3,[4]]]])); 
// [1,2,3,4]

22. Implement debounce
function debounce(func, delay) {
  let timer;
  return function(...args) {
    clearTimeout(timer);
    timer = setTimeout(() => func(...args), delay);
  };
}
const log = debounce(() => console.log("Debounced!"), 1000);
log(); log(); log(); // runs once after 1s

23. Implement throttle
function throttle(func, limit) {
  let lastCall = 0;
  return function(...args) {
    let now = Date.now();
    if (now - lastCall >= limit) {
      lastCall = now;
      func(...args);
    }
  };
}
const logThrottled = throttle(() => console.log("Throttled!"), 1000);
setInterval(logThrottled, 200); // logs once per second

24. Deep clone an object
function deepClone(obj) {
  return JSON.parse(JSON.stringify(obj));
}
let original = {a:1, b:{c:2}};
let clone = deepClone(original);
clone.b.c = 99;
console.log(original.b.c); // 2

25. Find all pairs with target sum
function pairSum(arr, target) {
  let pairs = [];
  for (let i = 0; i < arr.length; i++) {
    for (let j = i+1; j < arr.length; j++) {
      if (arr[i] + arr[j] === target) {
        pairs.push([arr[i], arr[j]]);
      }
    }
  }
  return pairs;
}
console.log(pairSum([1,2,3,4,5], 6)); 
// [[1,5],[2,4]]

26. Binary search
function binarySearch(arr, target) {
  let left = 0, right = arr.length - 1;
  while (left <= right) {
    let mid = Math.floor((left + right) / 2);
    if (arr[mid] === target) return mid;
    else if (arr[mid] < target) left = mid + 1;
    else right = mid - 1;
  }
  return -1;
}
console.log(binarySearch([1,2,3,4,5], 4)); 
// 3

27. Maximum subarray sum (Kadane’s Algorithm)
function maxSubarray(arr) {
  let max = arr[0], curr = arr[0];
  for (let i = 1; i < arr.length; i++) {
    curr = Math.max(arr[i], curr + arr[i]);
    max = Math.max(max, curr);
  }
  return max;
}
console.log(maxSubarray([-2,1,-3,4,-1,2,1,-5,4])); 
// 6

28. Shuffle an array randomly
function shuffle(arr) {
  for (let i = arr.length - 1; i > 0; i--) {
    let j = Math.floor(Math.random() * (i + 1));
    [arr[i], arr[j]] = [arr[j], arr[i]];
  }
  return arr;
}
console.log(shuffle([1,2,3,4,5]));
// Random order

29. Longest substring without repeating chars
function longestSubstring(s) {
  let set = new Set(), left = 0, max = 0;
  for (let right = 0; right < s.length; right++) {
    while (set.has(s[right])) {
      set.delete(s[left]);
      left++;
    }
    set.add(s[right]);
    max = Math.max(max, right - left + 1);
  }
  return max;
}
console.log(longestSubstring("abcabcbb")); 
// 3 ("abc")

30. Memoization (Factorial/Fibonacci)
function memoize(fn) {
  let cache = {};
  return function(n) {
    if (cache[n]) return cache[n];
    let result = fn(n);
    cache[n] = result;
    return result;
  };
}

// Factorial with memoization
const fact = memoize(function f(n) {
  return n <= 1 ? 1 : n * f(n - 1);
});
console.log(fact(5)); // 120

// Fibonacci with memoization
const fib = memoize(function f(n) {
  return n <= 1 ? n : f(n - 1) + f(n - 2);
});
console.log(fib(10)); // 55